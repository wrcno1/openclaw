import { SessionManager as SessionManagerValue } from "./session-transcript-contract.js";
import type { SessionManager as SessionManagerType } from "./session-transcript-contract.js";

export const SessionManager = SessionManagerValue;
export type SessionManager = SessionManagerType;
